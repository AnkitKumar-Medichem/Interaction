{
  "entities": {
    "QueryLog": {
      "title": "QueryLog",
      "description": "Log entry for an interaction query with SMILES inputs and predicted impurities",
      "type": "object",
      "properties": {
        "primaryCompound": { "type": "string", "description": "Primary compound SMILES" },
        "secondaryCompounds": { "type": "string", "description": "Secondary compound(s) SMILES if provided" },
        "predictedImpurities": { "type": "string", "description": "Summary of predicted reaction and degradation impurities" },
        "timestamp": { "type": "string", "format": "date-time", "description": "ISO timestamp of query execution" },
        "method": { "type": "string", "description": "Prediction method applied" }
      },
      "required": ["primaryCompound", "timestamp"]
    },
    "LogbookCsv": {
      "title": "LogbookCsv",
      "description": "Consolidated CSV logbook document storing the most recent 100 queries",
      "type": "object",
      "properties": {
        "csvContent": { "type": "string", "description": "Formatted CSV text of recent 100 queries" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "entryCount": { "type": "number", "description": "Total queries in logbook, capped at 100" }
      },
      "required": ["csvContent", "updatedAt"]
    }
  },
  "firestore": {
    "/query_logs/{logId}": {
      "schema": "QueryLog",
      "description": "Logbook records of queries maintained up to the most recent 100 entries"
    },
    "/logbook/{docId}": {
      "schema": "LogbookCsv",
      "description": "Maintained CSV logbook document storing the most recent 100 queries"
    }
  }
}
